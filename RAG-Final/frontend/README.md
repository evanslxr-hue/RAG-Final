# PDF RAG Assistant Frontend

React + Vite + TypeScript + Tailwind frontend for the local-first PDF RAG Assistant.

## Quick Start

```bash
npm install
cp .env.example .env
npm run dev
```

Vite runs on `http://localhost:5173` and talks to backend via `VITE_API_URL`.
